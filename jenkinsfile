
pipeline {

    agent {
        docker {
            image 'ghcr.io/cirruslabs/flutter:3.13.0'
            // Opcional: También puedes especificar otros detalles como etiquetas, argumentos, etc.
            // args '-v /ruta/local:/ruta/contenedor' // Mapeo de volúmenes
        }
    }
    // agent 'agent-docker'

    stages {
        // stage('Setup') {
        //     steps {
        //         sh 'pwd'
        //         sh 'ls'
        //         sh 'docker run -v /home/jenkins/workspace/prueba:. ghcr.io/cirruslabs/flutter:3.13.0 -it'
        //     }
        // }
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install dependencies') {
            steps {
                sh 'flutter pub get'
            }
        }

        stage('Run tests') {
            steps {
                sh 'flutter test'
            }
        }

        stage('Build APK') {
            steps {
                sh 'flutter build apk'
            }
        }
    }

    /* post {
        always {
            // Acciones que se ejecutarán siempre
        }
        success {
            // Acciones si la construcción y las pruebas tienen éxito
        }
        failure {
            // Acciones si la construcción o pruebas fallan
        }
    } */
}
